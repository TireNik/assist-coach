spring:
  datasource:
    url: jdbc:postgresql://localhost:6433/assistcoach
    username: dbcoach
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
